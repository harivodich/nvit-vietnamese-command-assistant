[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "nvit-vietnamese-command-assistant"
version = "0.1.0"
description = "Learning project for Vietnamese spoken-command understanding."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

